# sudo apt-get install python3-venv
# python3.7 -m venv venv

# pip install pip --upgrade
# pip install -r requirements-dev.txt

# install the current package in editable mode
# pip install -e .

# to store git credentials:
# git config credential.helper store
#  then do a git command, enter credentials, and they should be saved after that

pre-commit==2.8.1 # run 'pre-commit install' initially to install the git hooks
pytest==6.1.2
pytest-cov==2.10.0
pytest-randomly==3.4.1
#pytest-mock==3.1.1
sphinx==3.3.0
pylint==2.6.0
pytest-pylint==0.18.0
zest.releaser[recommended]==6.22.1

pytz==2020.4
